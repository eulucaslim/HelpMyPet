import styles from './styles.module.scss';

export function SubmitedButton(){
    return (
        <button type="button" className={styles.submitedButton}>
            Entrar
        </button>
    );
}